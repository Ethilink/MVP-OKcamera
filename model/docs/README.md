# docs

Context and reference docs for `model/` — for humans and agents alike. Design notes, dataset/labeling conventions, training decisions, anything worth writing down that isn't obvious from the code itself.

Not for product/client context — that lives in the `company-brain` vault (see repo-root `AGENTS.md`).
