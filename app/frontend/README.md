# app/frontend

Control screen for the ORC demo: Start/Stop, live camera view, results dashboard (Usage/Completeness per instrument). Talks only to `app/backend`'s API, never to `model/` directly.

Not yet scaffolded — framework choice (Vite vs. Next.js, both with React + TS + shadcn/ui) is still open.
