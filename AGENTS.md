# Agent instructions — ORC (OR Camera) MVP

Project context lives in the `company-brain` Obsidian vault, under `workspaces/surgical-sets` — not in this repo.

Don't import this context by default — only pull it when a task actually needs it, to avoid bloating context on every prompt. When needed, search/explore/read the specific file(s) that answer the question at hand, via the vault's MCP server if connected, or by reading the vault folder directly if you know its path. Neither available? See [`docs/obsidian-vault-setup.md`](docs/obsidian-vault-setup.md) — ask before changing local config.
